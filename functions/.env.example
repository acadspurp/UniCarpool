# Set these as Firebase Functions secrets (recommended):
# firebase functions:secrets:set RESEND_API_KEY
# firebase functions:secrets:set RESEND_FROM

# Option 1: Resend (https://resend.com) — recommended
RESEND_API_KEY=
RESEND_FROM=UniCarpool <onboarding@resend.dev>

# Option 2: Gmail SMTP (use an App Password)
OTP_SMTP_HOST=smtp.gmail.com
OTP_SMTP_PORT=465
OTP_SMTP_SECURE=true
OTP_SMTP_USER=
OTP_SMTP_PASS=
OTP_SMTP_FROM=
